#include <stdio.h>
#include "main.h"
/**
 * main - prints name + \n
 * @argc: number of arguments typed
 * @argv: array to arguements
 * Return: Success
 */
int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);
	return (0);
}
